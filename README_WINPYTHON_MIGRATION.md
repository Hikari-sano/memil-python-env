# WinPython-only migration

The project is moving to:

```text
WinPython only + one .venv per AI tool
```

Main entry points:

```text
Start.bat
AI_CATALOG.bat
SHARE_ENV_TO_AI.bat
```

Old `python/uv` is no longer required.
